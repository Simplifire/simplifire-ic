type Document = record {
  id: text;
  doc_name: text;
  date_created: text;
}


service : {

  "greet": (text) -> (text) query;

  "increment": () -> ();
  "get": () -> (nat) query;
  "set": (nat) -> ();

  "adddoc": (text) -> (text) query;
 
}